# 组件目录

本目录存放可复用的通用组件，例如：

截至 2026-09-25，管理页面已落地，但这里尚未抽出下列通用组件；当前直接使用 Element Plus 的表格、表单、树与对话框，权限按钮在各页面通过 `usePermissionStore().hasPermission()` 控制。详见 [前端进度](../../PROJECT_SUMMARY.md)。

- 权限按钮组件（根据权限标识控制显示）
- 表格封装组件
- 表单封装组件
- 树选择组件
- 对话框组件

业务逻辑应保持在页面组件中，通用组件只负责 UI 展示和基础交互。
